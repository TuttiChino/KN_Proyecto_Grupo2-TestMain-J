@{
    Layout = "~/Views/Shared/_LayoutInterno.cshtml";
    ViewBag.Title = "Usuarios | Admin";
}

<div class="container mt-4">
    <div class="row mb-4">
        <div class="col-md-12">
            <h2>Gesti&oacute;n de Usuarios</h2>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div class="alert alert-info">
                <i class="bi bi-info-circle"></i> Esta funcionalidad estar&aacute; disponible pr&oacute;ximamente.
            </div>
            <p>El m&oacute;dulo de gesti&oacute;n de usuarios se encuentra en desarrollo.</p>
            <a href="@Url.Action("Landing", "Home")" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Volver al Panel
            </a>
        </div>
    </div>
</div>

